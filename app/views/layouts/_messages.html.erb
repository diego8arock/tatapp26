<p id="alert"><%= alert %></p>
<p id="notice"><%= notice %></p>
<% flash.each do |name, msg| %>
  <!--<div class="alert alert-<%= name == :notice ? "success" : "error" %> flash">
    <a class="close" data-dismiss="alert" href="#">×</a>
    <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
  </div>-->
<% end %>
