<%= flash_messages %>
<div id="error_message" class="alert alert-danger fade in hidden">
  <button id="close-button" class="close" type="button" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">×</span>
  </button>
  <span id="error_message_txt">Welcome to the App26!</span>
</div>
<% flash.each do |name, msg| %>
  <!--<div class="alert alert-<%= name == :notice ? "success" : "error" %> flash">
    <a class="close" data-dismiss="alert" href="#">×</a>
    <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
  </div>-->
<% end %>
